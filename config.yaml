# Yuhuo Sync DB 配置文件示例

# 源数据库配置（测试/预发布环境）
source:
  host: 10.10.200.16
  port: 3306
  username: tms
  password: tms123456
  database: tms
  charset: utf8mb4

# 目标数据库配置（线上环境）
target:
  host: 10.10.200.6
  port: 3306
  username: tms
  password: tms123456
  database: tms
  charset: utf8mb4

# 需要同步数据的表列表（所有表默认比对结构）
sync_data_tables:

# 日志配置（可选）
logging:
  level: INFO
  file: sync.log
